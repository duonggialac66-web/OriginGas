
  # Gas Delivery Management System

  This is a code bundle for Gas Delivery Management System. The original project is available at https://www.figma.com/design/cBoiFDfIUn5qtCdedTl2m6/Gas-Delivery-Management-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  